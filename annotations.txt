Always start buy defining the outer functions,
afterwards define the lower level ones, along with
what is returned, alongside the return types

ord = ascii for number

import heapq for heaps
you apply its operation on a list, then you have
the needed heap

etc.sort() for simple lists
etc.sort(lambda x: EXPRESSION, Reverse=bool)
for sorting

if you are going to work with ordered lists, use
import bisect
bisect.insort(list, element)
if element is tuple or list, first part is used to order
and then you can use 
bisect.bisect_left(list, target)
↓→ will put at the first opportunity
bisect.bisect_right(list, target)
↓→ will put at the last opportunity

tuples can be defined as
t = (a, b, c)
and accessed as t[0], t[1], ...

if you are dealing with a graph problem, the
default way to create an adjency list is using
from collections import defaultdict
graph = defaultdict(list)
#list argument makes an origin node be created
#automatically when executing the line below
#the argument defines the behaviour of the 
#default dict, being a set, list or int
#after accesing the first key
graph[origin].append(destiny) #storing a new edge
print(graph[origin]) #acessing the 
print

lists in python are not linked lists, like you
used to think, but instead are actually arrays in
their implementation.

when summing the values of a list, use
sum(list)
It uses a built in python function which runs
as C

lambda in python is a one liner, one use function
for example:
x = lambda a: a+120*a*a
or can also be
y = lambda a,b,c: a+c*b+c
they then can be invoked as
x(2)
y(2,3,4)

remember to think about edge cases in order to
clarify the problem you want to solve

zip can merge n lists creating a list of n-uples.
It only creates the items when necessary, and discards
the items when they are used. because of that, also using
list() is essencial in a lot of times;

dijkstra is implemented by having a heap with
the total time to reach each destiny. When a destiny is popped
from the heap, it is the closest path to reach it. Storing 
the visited nodes through the heap can also help with optimization
